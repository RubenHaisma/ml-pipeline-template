"""mlt — CLI-first ML pipeline template.

The house style for a set of production ML reference repos:
CLI-first, ``--json`` on every command, load-bearing exit codes,
MLflow as the single source of truth, marimo (not Jupyter) for exploration.
"""

__version__ = "0.1.0"
